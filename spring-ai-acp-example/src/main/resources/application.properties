spring.application.name=travel-agent

# Credentials and region come from the AWS default chain (env vars, then ~/.aws default
# profile). Pin a profile/region here if you need to, e.g.:
#   spring.ai.bedrock.aws.profile.name=default
#   spring.ai.bedrock.aws.region=us-east-1
spring.ai.bedrock.converse.chat.model=global.anthropic.claude-opus-4-6-v1
